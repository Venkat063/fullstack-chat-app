{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Fullstack Chat Application (MERN Stack)",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run install-all && cd frontend && npx vite build",
    "start": "npm run start backend && npm run dev frontend",
    "dev": "concurrently \"npm run dev backend\" \"npm run dev frontend\""
  },
  "keywords": [
    "chat",
    "mern",
    "fullstack",
    "socket.io",
    "react",
    "express"
  ],
  "author": "Venkat",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
